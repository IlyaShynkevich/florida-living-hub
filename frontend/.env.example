# Leave unset for local dev — requests stay relative and go through the
# Vite dev proxy defined in vite.config.js.
# Set this when deploying the frontend and backend as separate services,
# pointing at the backend's public origin (no trailing slash).
# VITE_API_BASE_URL=https://api.example.com
